<?xml version="1.0" encoding="UTF-8" ?>
<project name="copy" basedir="." default="init">

    <property file="copy.properties"/>

    <!-- 将自动生成的包 拷贝到真实项目的开发环境 -->
    <target name="init">
        <copydir src="${srcDir}" dest="${destDir}" forceoverwrite="true"  />
    </target>
</project>